# Disabled Workflows

These files are archived for reference only. GitHub Actions only discovers
workflow files under `.github/workflows/`, so archived workflows use the
`.yml.disabled` extension and live outside that directory.

Do not move these files back into `.github/workflows/` or rename them to `.yml`
unless the project owner explicitly asks to re-enable CI workflows.